// allowedOrigins states the sites that have access to server data (can do without if want to allow public to have access)
const allowedOrigins = ['https://www.google.com', 
    'http://127.0.0.1:5000', 
    'http://localhost:3500'
]

module.exports = allowedOrigins